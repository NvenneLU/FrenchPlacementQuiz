import { OptionValidDirective } from './option-valid.directive';

describe('OptionValidDirective', () => {
  it('should create an instance', () => {
    const directive = new OptionValidDirective();
    expect(directive).toBeTruthy();
  });
});
